{
  "name": "rpg-juego",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend -- --production && npm install --prefix backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
